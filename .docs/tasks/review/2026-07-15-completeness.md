# COMPLETENESS REPORT — «Потренировать ещё» (v1.1 «Направленный добор»)

**ТЗ**: `/Users/meriler/cc/vault/itmo/ai-school/01-воркшоп/дизайн-потренировать-ещё.md`
(секция «Мини-ТЗ на реализацию v1.1», 10 пунктов + капканы C1–C7 из «РАУНД 2 КРИТИКИ» + техкапканы F1/F2/F3/F5 из «СИНТЕЗ КРИТИКИ»)
**Задачи**: `/Users/meriler/cc/code/itmo/ai-school-workshop/.docs/tasks/` (TASK-1…7 + INDEX.md)

Требований в ТЗ (в скоупе проверки): **21** = 10 пунктов мини-ТЗ + 7 капканов C1–C7 + 4 техкапкана F1/F2/F3/F5.
Задач: **7** (+ INDEX.md с таблицей покрытия).

## FORWARD TRACEABILITY (ТЗ → задачи)

Покрыто: **21 / 21**
Частично: **0**
НЕ покрыто: **0**

Все 10 пунктов мини-ТЗ v1.1 и все 11 обязательных капканов (C1–C7, F1, F2, F3, F5) реализованы хотя бы одной задачей с конкретным, тестируемым acceptance criterion (не просто упоминанием — с указанием файла/строки v5.html, TDD-якорем и конкретным текстом проверки). Карта:

| Требование | Задача(и) | Комментарий |
|---|---|---|
| п.1 вход/eligibility [F5] | TASK-2 | AC: eligibility по outcome∈{fixed,partial}+валидный res2, доступно при 4/4, скрыто при no_break/inconclusive |
| п.2 экран/плитки/«меньше всего» [C1] | TASK-1, TASK-2 | TASK-1 — quadCounts()/recommendedTiles(); TASK-2 — текст метки, подписи по расстоянию |
| п.3 сбор/якоря/реестры [F1,F2,C5] | TASK-2 (реестры), TASK-3 (сбор, guard, обход nextAfterTask) | |
| п.4 проверка обязательна [F3,C2] | TASK-2 (менюToFinal только при 0 наборов), TASK-4 (menuRequired скрывает #ckExit) | |
| п.5 лучший счёт [C3] | TASK-1 (betterRes), **TASK-4** (MENU.best апдейт, outcome-неградация), TASK-5 (печать) | INDEX.md не упоминает TASK-4 — см. находку ниже |
| п.6 лимит цикла [C7] | TASK-3 | |
| п.7 stuck 15с [C6] | TASK-3 | |
| п.8 автосейв [C4] | TASK-6 | |
| п.9 разграничение поверхностей [F8] | TASK-2 | AC/TDD не проверяют формулировку явно — см. находку ниже |
| п.10 регресс/e2e | TASK-7 | список сценариев дословно соответствует перечню п.10 |
| C1 | TASK-1, TASK-2 | |
| C2 | TASK-4 | |
| C3 | TASK-4, TASK-5 | |
| C4 | TASK-6 | |
| C5 | TASK-2 | |
| C6 | TASK-3 | |
| C7 | TASK-3 | |
| F1 | TASK-3 | |
| F2 | TASK-3 | |
| F3 | TASK-4 | |
| F5 | TASK-2 | |

Непокрытых требований нет.

## REVERSE TRACEABILITY (задачи → ТЗ)

Связано с ТЗ: **7 / 7**
Scope creep: **0**

Все 7 задач прослеживаются к конкретным пунктам/капканам мини-ТЗ v1.1. Технические решения, которых нет дословно в тексте ТЗ (имена функций `quadCounts`/`recommendedTiles`/`betterRes`, объект `MENU`, карта `QUAD`, номер seat=93 для e2e, разделение счётчиков `MENU.taps`/`MENU.cycleSets`, место хранения снапшота `g.menu`, ранжирование outcome `{partial:1,fixed:2}`) — это необходимая инженерная детализация требований ТЗ (в первую очередь капканов C1, C3, C7, C4), а не самостоятельный незапрошенный функционал. Каждое такое решение в задачах явно привязано к конкретному пункту/капкану и обосновано цитатой из ТЗ. «Возможное v2» (шаг предсказания) нигде не реализовано — граница v1/v2 соблюдена.

Отдельно проверено расширение п.5 (лучший счёт) на `outcome` (fixed/partial), а не только на числовой счёт — в тексте ТЗ п.5 говорит только о `bestRes2`/заголовке-числе, но капкан C3 прямо описывает симптом «получит 2/4 → "Почти починил" в заголовке» — то есть неградация `outcome` необходима, чтобы заголовок не откатывался, и является корректной интерпретацией C3, а не creep.

SCOPE CREEP: не обнаружен.

## СВЕРКА ТАБЛИЦЫ ПОКРЫТИЯ INDEX.md С СОДЕРЖИМЫМ ЗАДАЧ

1. **[MINOR] Строка «п.5 лучший счёт [C3]» в INDEX.md указывает только `TASK-1 (хелперы), TASK-5`, пропуская TASK-4.**
   → Сама задача TASK-4 в шапке прямо пишет: «Покрывает мини-ТЗ v1.1 п.4… и п.5 в части `MENU.best` (C3)», и её AC/TDD содержат обновление `MENU.best=betterRes(...)` и неградацию `outcome` — то есть TASK-4 реализует существенную часть механики «лучшего счёта», а не только TASK-1/TASK-5.
   → Fix: дописать в INDEX.md таблицу покрытия строку п.5 как `TASK-1 (хелперы), TASK-4 (MENU.best апдейт, outcome-неградация), TASK-5 (печать)`.

Остальные 9 строк таблицы INDEX.md сверены с фактическим содержимым задач и соответствуют один в один (проверены заголовки задач, разделы «Описание» и AC на предмет заявленных капканов).

## ГЛУБИНА РЕШЕНИЯ

Задачи не перефразируют ТЗ — везде даны конкретные файлы/строки `v5.html`, конкретные имена функций/полей, TDD-якоря с точными входными данными и ожидаемым текстом. Ошибочные пути учтены избирательно: TASK-3 покрывает мягкий stuck (некорректный гейт), TASK-6 — обратную совместимость старого снапшота (защита от исключения при `resumeFlow`), TASK-4 — «тихий выход» как явно закрытая дыра. Валидация входных данных (eligibility-гейт TASK-2, лимит наборов TASK-3) предусмотрена. Логирование/телеметрия (`TELE.push('menu_set_done', ...)`, `TELE.push('menu_gate_stuck', ...)`) заведена там, где по аналогии с остальным кодом это уместно.

Найдено 2 точечных недочёта — требование реализуется кодом, но не имеет собственного тестируемого acceptance criterion (риск, что деталь потеряется при реализации без строгого TDD-надзора):

2. **[MINOR] TASK-2, п.2 ТЗ: строка призыва «ты — тренер» есть в разделе «Что сделать» (создание `#menu` с «строка призыва «ты — тренер»»), но отсутствует в Acceptance Criteria и TDD-якоре TASK-2.**
   → Ни один assert в TDD-секции TASK-2 не проверяет наличие этого текста на экране.
   → Fix: добавить в AC/TDD TASK-2 пункт вида `ok('экран содержит призыв «ты — тренер»', ...)`.

3. **[MINOR] TASK-2, п.9 ТЗ / капкан F8 (разграничение поверхностей): пункт зафиксирован в «Технических заметках» TASK-2 («Меню — "докорми и проверь"… Лаборатория… не смешивать тексты и не называть обе "тренер"»), но не имеет собственного AC/TDD-ассерта.**
   → Разграничение — единственный из 21 требования пункт без хотя бы одного явного теста (все остальные 20 имеют минимум по одному TDD-ассерту).
   → Fix: добавить простой ассерт — текст `#menu` не содержит слова «Лаборатория», а текст экрана Лаборатории (если легко проверяется) не содержит слова «тренер» рядом с призывом меню; либо, как облегчённый вариант, просто зафиксировать текстовые константы копирайта отдельным ассертом.

Ни один из этих 2 недочётов не является пропущенным требованием в смысле «не реализовано» — реализация заявлена в «Что сделать» обеих задач, отсутствует только тестовое покрытие конкретно этой детали. Понижены до MINOR (не CRITICAL/MAJOR), т.к. риск потери при разработке низкий (это копирайт-константы, не логика с ветвлениями).

## OVER/UNDERENGINEERING

- **Overengineering: нет.** 7 задач на 21 требование — пропорционально (не раздуто), каждая задача закрывает кластер логически связанных пунктов/капканов волнами с явной зависимостью, а не искусственно раздроблена. Абстракций «на будущее» не найдено — «Возможное v2» (шаг предсказания) явно НЕ реализуется ни в одной задаче, граница соблюдена. Разделение `MENU.taps` (session-wide) и `MENU.cycleSets` (per-cycle) — не лишний слой, а прямое следствие недоопределённости C7, снятой самим документом ТЗ.
- **Underengineering: нет системного.** Error handling (stuck-таймер, обратная совместимость снапшота, защита от «тихого выхода»), валидация (eligibility-гейт, лимит наборов) и логирование (TELE-события) предусмотрены. Два точечных недочёта тестового покрытия описаны выше (MINOR #2, #3) — это не пробел в реализации, а пробел в верификации отдельных копирайт-деталей.

## СТАТУС: COMPLETE — все требования покрыты (с 3 MINOR-находками: 1 расхождение таблицы INDEX.md, 2 нехватки тестового покрытия для копирайт-деталей)

### Счётчики находок
- Непокрытые требования (форвард): **0**
- Scope creep (реверс): **0**
- Расхождения INDEX.md ↔ задачи: **1** (MINOR)
- Прочие находки (глубина/тестовое покрытие): **2** (MINOR)
- Всего находок: **3**, все уровня MINOR — блокеров к реализации нет.
