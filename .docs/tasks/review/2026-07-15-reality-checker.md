# REALITY CHECK REPORT — «Направленный добор» (TASK-1..7)

Дата проверки: 2026-07-15
Проверено task-файлов: 7 (+ INDEX.md)
Источник требований: `/Users/meriler/cc/vault/itmo/ai-school/01-воркшоп/дизайн-потренировать-ещё.md` (секции «РАУНД 2 КРИТИКИ» + «Мини-ТЗ v1.1»)
Кодовая база: `/Users/meriler/cc/code/itmo/ai-school-workshop/v5.html` (2141 строка), `e2e/*.mjs`

## Метод

Построчно сверено КАЖДОЕ упоминание `v5.html:NNNN` во всех 7 задачах (≈45 ссылок) против реального
содержимого файла на эту строку; проверены имена функций/объектов/CSS-классов/HTML-id, которые задачи
объявляют «существующими» (не новыми); проверены claims о содержимом `e2e/*.mjs`; проверена
внутренняя согласованность плана между задачами (кто что сохраняет/читает/сериализует).

## Итог

**Точность построчных ссылок на код — очень высокая.** Из ~45 проверенных пар `v5.html:NNNN` +
«что там написано» — **все совпали дословно или почти дословно** (включая тонкие детали вроде
`GUIDE.unlocked` бага, `stuckMs>25000` на :1121, `SAVE_RANK` на :793, `Object.assign(GUIDE,{...s.g})`
на :854 и т.д.). Заметно, что задачи писались с реальным чтением файла (вероятно, Codex round 2),
а не по памяти. Найденные проблемы — не в ссылках на существующий код, а в (а) одном фактическом
мираже про содержимое e2e-теста и (б) внутренних нестыковках ПЛАНА между задачами.

Найдено: **0 critical, 4 major, 2 minor**.

---

## TASK-1: Логика данных меню — квадранты, рекомендации, лучший счёт

**Статус**: OK (с 1 minor)
Проверено: `CFG.needR1/needR2` (:473) ✓, `DBG` (:480) ✓, `GUIDE` (:922-929) ✓, `TASKS` (:930-939, включая
точное соответствие cls/gate по каждому из 4 квадрантов) ✓, `renderDebug()` (:2084) ✓.

- **[MINOR]** Хинт «под `if(DBG){...}` пробросить в `window`» подразумевает существующий блок
  для реиспользования. В реальности `if(DBG)` встречается в коде РОВНО один раз — однострочный вызов
  `if(DBG)renderDebug(p);` внутри тикающей функции `classifyTick()` (:770), а не блок одноразовой
  инициализации при загрузке. Разработчику нужно СОЗДАТЬ новый `if(DBG){...}` рядом с объявлением
  `MENU`, а не переиспользовать существующий. Тривиально обходится, но хинт неточен.
  → Переформулировать: «добавь новый `if(DBG){ window.MENU=...; }` сразу после объявления MENU».

---

## TASK-2: Экран меню и вход с финала

**Статус**: NEEDS_FIX (1 major)
Проверено: разметка `#final`/`#fMore` (:400-415, кнопка `#fMore` дословно на :407) ✓, обработчик
`$('#fMore').onclick` (:2041, дословное совпадение включая `Object.assign(TASK_INTRO.t2a,T2A_NEUTRAL)`) ✓,
условие видимости `#fMore` (:1690, дословное совпадение) ✓, `SCREENS` (:912) ✓, `NAV_SCREENS`/`NAV_SCR`
(:974-976) ✓, `showFinal()`/`GUIDE.outcome/res2/res1` (:1653-1657) ✓, CSS `.tile`/`.choice` реально
существуют (:123, :157-163) ✓.

1. **[MAJOR]** Файлы для изучения утверждают: «`e2e/e2e-postlesson.mjs` — … его ассерты про старый
   `#fMore` нужно обновить под новое поведение», и Acceptance Criteria требует «`e2e-postlesson.mjs`
   обновлён под новое поведение `#fMore`». Проверка (`grep -n "fMore" e2e-postlesson.mjs`) — **0
   совпадений**. `#fMore` вообще не упоминается ни в одном файле `e2e/*.mjs` в репозитории. Реального
   регресс-покрытия поведения `#fMore` СЕЙЧАС НЕТ нигде — значит нечего «обновлять», а изменение
   поведения кнопки TASK-2 остаётся непокрытым существующим регрессом (только новым `e2e-menu.mjs`).
   → Убрать формулировку «обновить старые ассерты о рельсе `#fMore`» — их не существует; явно указать,
   что регресс на новое поведение `#fMore` целиком ложится на `e2e-menu.mjs`.

---

## TASK-3: Сбор в меню — 3 фото, возврат в меню, якоря, stuck, лимит

**Статус**: OK
Проверено: `startTask()` (:1098-1110) ✓, `tickGuidedTeach()` (:1111-1131, включая точное совпадение
`stuckMs>25000` на :1121 и блока завершения `T.got>=T.need` на :1127-1128) ✓, `nextAfterTask()`
(:1132-1138, рельса `t2a→startTask('t2b')` на :1136 дословно) ✓, `navToHub()`/`navResumeLesson()`
(:1000-1029) ✓, `navCapture()` ветка gteach (:982-983) ✓, восстановление gteach в `navResumeLesson`
(:1019, дословное совпадение) ✓, guard в `navGo` блокирующий shoot/range/prize при kind gteach/gcheck
(:1045, дословное совпадение — это РЕАЛЬНО существующий guard, не выдумка) ✓, `NAV.lesson=null` после
финала (:1656) ✓. Демо-хелпер `size` → `lastHand.side=D.size*0.55` (:622) подтверждает корректность
примеров кадров в TDD-якорях (`size:0.3`→gate small, `size:1.0`→gate big).

技нических претензий к самой задаче не найдено — она внутренне последовательна и физически
реализуема на существующей архитектуре.

---

## TASK-4: Обязательная проверка меню без выхода

**Статус**: OK
Проверено: `startGuidedCheck(round)` (:1184-1193, строка :1190 `const optional=round===2&&!!GUIDE.outcome;`
дословно совпадает) ✓, `finishGuidedCheck()` (:1309-1330, ветка round===2 на :1322-1329, `GUIDE.res2=`
на :1323, установка outcome на :1324 — дословно) ✓, `guidedCards(2)` реально возвращает 4 карточки
(:1149-1152) ✓, `CHK` структура (:1186) допускает добавление `menuRequired` без конфликтов ✓,
`#ckExit` изначально `hidden` в HTML (:231) ✓.

---

## TASK-5: Отображение результата — лучший счёт

**Статус**: NEEDS_FIX (1 major, зависит от связки TASK-2+TASK-4)
Проверено: `showFinal()` ветка fixed/partial (:1670-1678, score-строка :1674 дословно) ✓, `#fscore`/
`#freveal` реальные существующие узлы (:403-404) ✓, обработчики `#fCert`/`#gCert` (:2036/:2000-2001,
`r=GUIDE.res2||GUIDE.res1` дословно) ✓, образец e2e :29-159 и download-паттерн :193-197 — оба точные.

1. **[MAJOR]** Логическая дыра в связке TASK-2 → TASK-4 → TASK-5, не покрытая НИ ОДНИМ TDD-якорем
   ни в одной из 7 задач. TASK-4 явно допускает повторный вход в меню («После проверки menu-цикл
   закрыт … повторный вход с финала снова доступен»). Но TASK-2's `openMenu()` при КАЖДОМ входе
   безусловно делает `MENU.best=GUIDE.res2` (не через `betterRes`). Сценарий: baseline 4/4 → добор
   регрессировал до 2/4 → `MENU.best=betterRes(4/4,2/4)=4/4` (пик держится корректно внутри ПЕРВОГО
   цикла) → ребёнок жмёт «Потренировать ещё» ВТОРОЙ раз → `openMenu()` выполняет `MENU.best=
   GUIDE.res2=2/4`, **затирая** ранее сохранённый пик 4/4 текущим (регрессировавшим) значением.
   Это напрямую противоречит цели C3/TASK-5 «печатать ЛУЧШИЙ счёт, никогда не регресс» — но
   поскольку все TDD-якоря во всех задачах гоняют ровно ОДИН цикл меню, баг пройдёт мимо зелёного
   прогона тестов.
   → В TASK-2 заменить `MENU.best=GUIDE.res2` на `MENU.best=betterRes(MENU.best,GUIDE.res2)` (не
   обнулять пик при повторном входе); в TASK-7 добавить TDD-сценарий «второй #fMore-цикл после
   первого успешного» с проверкой, что пик не откатывается.

---

## TASK-6: Автосейв и восстановление menu-добора

**Статус**: NEEDS_FIX (2 major)
Проверено: `saveMilestone()` (:800-824, тело снапшота :807-812 дословно) ✓, `SAVE_RANK` (:793,
дословно) ✓, ранг-защита (:805, дословно) ✓, `loadSave()` (:825-829, валидация дословна) ✓,
`applySave()` (:831-857, `Object.assign(GUIDE,{...s.g})` на :854 дословно) ✓, `resumeFlow()`
(:1955-1973, ветки final/fixdata/revealed/else на :1967/:1968/:1971/:1972 — все 4 дословно совпали) ✓,
`addExample` дебаунс 1200мс (:890, дословно) ✓, `trySaveOn()` (:860-867, дословно) ✓.

Построчная точность здесь безупречна — но сам ПЛАН сериализации внутренне неполон:

1. **[MAJOR]** Снапшот сериализует только `g.menu={pending, taps, baseline, best}`. Но TASK-1
   объявляет `MENU` с ОБЯЗАТЕЛЬНЫМИ полями `cycleSets` (лимит «максимум 2 набора» — acceptance
   criteria TASK-3, C7) и `lastCheckAt` (маркер для реплики TASK-5). Ни то, ни другое не входит в
   снапшот и не восстанавливается в `applySave`. После F5 посреди цикла `cycleSets` обнулится →
   ребёнку СНОВА откроются все плитки-добора, хотя до перезагрузки он уже исчерпал лимит 2 набора —
   прямое нарушение acceptance criteria TASK-3 «Максимум 2 набора до проверки».
   → Добавить `cycleSets` и `lastCheckAt` в состав `g.menu` и в восстановление `applySave`.

2. **[MAJOR]** Несогласованность между TASK-3 и TASK-6 о том, КТО добавляет синхронный вызов
   `saveMilestone('final')` сразу по завершении набора 3/3 (обход дебаунса 1200мс, C4). TASK-6
   пишет: «Точка вызова добавляется в TASK-3» — но чек-лист TASK-3 «Что сделать» (блок завершения
   `tickGuidedTeach`) перечисляет `MENU.taps.push(...)`, `TELE.push('menu_set_done',...)`,
   `MENU.pending=true`, `openMenu()` — вызова `saveMilestone(...)` там НЕТ. Если оба таска
   реализуются строго по своим чек-листам, синхронный сейв не будет добавлен вообще → TDD-якорь
   TASK-6 «набор попадает в снапшот СРАЗУ (без ожидания 1,2 с)» упадёт.
   → В TASK-3 явно добавить пункт «вызвать `saveMilestone('final')` синхронно сразу после
   `MENU.taps.push(...)`», либо в TASK-6 явно указать это как правку самого TASK-3 (а не TASK-6).

---

## TASK-7: Полный регресс

**Статус**: NEEDS_FIX (1 major, наследуется из TASK-2; 1 minor)
Проверено: таблица сценариев (файл/что проверяет/~ассертов/seat) сверена построчно с реальными
файлами — **все числа ассертов (`grep -c "ok("`) совпали ТОЧНО**: e2e-ws 30, e2e-home 10, e2e-hub 18,
e2e-postlesson 37, e2e-resume 8, e2e-resume2 9, e2e-uimatrix 38, e2e-ws-prod 29. Seat-номера
(94/91/95/97/98) подтверждены построчным grep по `?seat=` в соответствующих файлах — точное
совпадение. `run-e2e.mjs:17` (exit-код по FAIL/EXCEPTION) — дословное совпадение. Chromium-кэш
`chromium-1223` реально существует на диске.

1. **[MAJOR]** (наследуется из TASK-2) «Технические заметки» повторяют тот же мираж: «`e2e-postlesson.mjs`
   содержит старые ассерты рельсы `#fMore` (`:100–121`) … если он всё ещё гоняет старую рельсу — это
   баг незавершённого TASK-2». Строки 100–121 в реальности тестируют экран «Выбор починки» (`#fixpick`,
   выбор между «Ещё таких же»/«Наоборот»), НЕ имеющий отношения к `#fMore`/«Потренировать ещё».
   `#fMore` не упоминается в файле нигде. Инструкция «вернуть на доработку, если рельса ещё
   гоняется» отсылает к несуществующему сигналу.
   → См. фикс для TASK-2 выше — общий источник миража.

2. **[MINOR]** Таблица (и TASK-1) утверждают «seat 93 — свободен» для `e2e-menu.mjs`. Фактически
   `e2e/shots-hub.mjs:16` уже использует `seat=93` (`http://localhost:8642/v5.html?demo=1&ws=1&seat=93`).
   Практический риск низкий — `shots-*.mjs` явно исключены из регресс-цикла (сказано в самом TASK-7:
   «утилиты скриншотов, НЕ регрессионные тесты»), и каждый запуск `run-e2e.mjs` стартует Chromium с
   чистым эфемерным профилем (без `--user-data-dir`), так что localStorage между отдельными
   `node run-e2e.mjs <file>` не персистентен — коллизии по факту не будет. Но фактическое
   утверждение «свободен» неверно относительно реального содержимого репозитория.
   → Уточнить формулировку: «свободен среди регрессионных сценариев; `shots-hub.mjs` тоже использует
   93, но это screenshot-утилита вне CI-цикла».

---

## КОНФЛИКТЫ МЕЖДУ ЗАДАЧАМИ

- **TASK-2 ↔ TASK-7**: общий мираж про содержимое `e2e-postlesson.mjs` (см. MAJOR в обеих секциях) —
  один и тот же фактически неверный claim, продублированный в двух файлах.
- **TASK-3 ↔ TASK-6**: неоднозначность владения строкой кода (синхронный `saveMilestone` при
  завершении набора) — обе задачи ссылаются друг на друга («точка вызова в TASK-3» / TASK-3 молчит
  об этом), риск, что вызов не появится ни в одной реализации.
- **TASK-1 ↔ TASK-6**: `MENU.cycleSets`/`MENU.lastCheckAt` объявлены как часть состояния в TASK-1, но
  не включены TASK-6 в план сериализации снапшота — рассинхрон между «что такое MENU» и «что из MENU
  переживает F5».
- **TASK-2 ↔ TASK-4 ↔ TASK-5**: `MENU.best` в TASK-2 обнуляется при каждом повторном `openMenu()`
  вместо накопления через `betterRes`, что подрывает гарантию TASK-5 «печатать лучший, никогда не
  регресс» при multi-cycle использовании «Потренировать ещё». Ни один TDD-якорь (TASK-1…TASK-7) не
  покрывает второй цикл меню — баг физически не будет пойман тестами как написано.
- Прямых конфликтов «две задачи трогают одно и то же место кода в одной волне» нет — волны строго
  последовательны (задача = волна), как и заявлено в INDEX.md, зависимости логичны и подтверждены
  кодом (TASK-2 действительно требует MENU из TASK-1 и т.д.).

---

## ИТОГО

**0 critical, 4 major, 2 minor → NEEDS_FIX** (не блокирует старт работы — построчные ссылки на код
исключительно точны, реализация физически выполнима; но 4 major-находки надо закрыть до/во время
реализации TASK-2, TASK-5, TASK-6, иначе часть acceptance criteria и TDD-якорей будет ложно-зелёной
или тесты будут искать несуществующий код).
