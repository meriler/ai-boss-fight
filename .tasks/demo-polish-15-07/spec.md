# ТЗ: доделка демки к занятию 15.07 (по итогам техпрогона 13.07)

> ✅ **ВЫПОЛНЕНО ночью 14.07** (вкл. опциональную задачу 6). Итоги, DoD-чек и утренний смок-чеклист → [report.md](report.md). Codex-ревью ×3 → `codex/out-r{1,2,3}.md`. Прод обновлён, Codex-вердикт SHIP.

**Для соседней сессии.** Занятие — СРЕДА 15.07 18:30 МСК. Дедлайн правок: 15.07 ~15:00, дальше полный смок и заморозка.

## Контекст (прочитать, не переспрашивать)

- Разбор прогона: `~/cc/vault/itmo/ai-school/01-воркшоп/разбор-прогона-2026-07-13.md` (выжимка расшифровки, критика Codex, статус реализации).
- **Рабочий файл — `v5.html`** (сборка среды). ⛔ **`v4.html` НЕ ТРОГАТЬ** — заморожен как прогонная версия, Алексей просил явно. Бот уже рассылает детям ссылки на v5 (`?ws=1&seat=N`).
- Код: `~/cc/code/itmo/ai-school-workshop/` (git есть, коммитов моих правок ещё нет — дифф большой, см. `git diff v5.html` бессмысленно, файл новый; база — v4 из HEAD).
- Деплой: `./deploy-ws.sh` (rsync на Aeza, HTML отдаётся no-store — правки видны по F5).
- E2e: `e2e/run-e2e.mjs <сценарий>` (playwright, локальный сервер `python3 -m http.server 8642` в корне проекта должен работать; сценарии e2e-ws.mjs — полный путь 30 PASS, e2e-resume.mjs — F5-восстановление 8/8; для v5 запускать через `sed 's/v4.html/v5.html/'` во временный файл — или поправь пути в сценариях на v5 насовсем, v4 больше не тестируем).
- Телеметрия/дашборд: не трогать (боевое). Тестовые места — ТОЛЬКО 90–99 (детские 1–89 склеятся с реальными детьми). Утренние проверки — `?demo=1` или seat 90+.

## Что УЖЕ сделано в v5 (не переделывать)

Автосейв на вехах + «⏩ Продолжить с места» (localStorage `ws_save_<seat>`, TTL 6ч, восстановление в безопасные точки: проверка R1 / разгадка / проверка R2 / финал) · темп сбора `sampleCdMs` 520→900 · кнопка fMore «Потренировать ещё (по желанию)» вместо «добить до 4/4» · подпись шкал уверенности · честное признание ловушки на экране разгадки · подсказка «звонок и тренажёр в разных ОКНАХ» на интро (ws) · лаборатория: клавиши 1/2 для сбора, кнопка «📸 Мои примеры» (галерея всех раундов), reset с двухшаговым подтверждением + очисткой сейва, онбординг с миссиями детектива · `ws_unlocked` привязан к месту.

## Задачи (по приоритету)

### 1. Экран вариантов объяснения — разгрузить (Настин скрин с прогона)
Жалоба: «много текста, нелогично выделен правильный ответ, на глаза попадается в последнюю очередь».
Сейчас (`#guessbtns`, ~строка 1250): 5 кнопок-вариантов длинными строками, порядок перемешивается, выделения правильного НЕТ (уже ок). Сделать:
- сократить тексты вариантов до ≤5 слов каждый (сохранив смысл и категории cat — они уходят в телеметрию/рубрику, ключи key/cat НЕ менять);
- визуально выровнять кнопки (одинаковая высота, крупнее шрифт);
- проверить экран разгадки `#reveal` — если и там простыня, ужать заголовок/ключ (`#revkey`).

### 2. Обратная связь на каждый собранный кадр
Сбор замедлен до ~0.9с/кадр — теперь между кадрами пауза, и нужен явный «щёлк»: краткая вспышка рамки/иконки 📸 на каждый принятый кадр (звук уже есть — `SFX.learn`). Смотреть `addExample()` и гейт-сбор `tickGuidedTeach`. Дёшево, не перестраивать.

### 3. Подсказки при починке (Настя: «нужны ещё варианты»)
В задачах починки t2a/t2b тексты просят «маленькую ладонь» / «большой кулак» по 3 кадра. Настя путалась: непонятно, участвуют ли старые примеры. Добавить в тексты шагов починки одну строку: «Старые примеры остаются в альбоме — ты ДОБАВЛЯЕШЬ новые» (см. `TASKS.t2a/t2b` и `T2A_NEUTRAL`).

### 4. Приватность и автосейв — согласовать формулировку
Демка обещает «видео не уходит с устройства» — это правда. Но автосейв теперь хранит миниатюры примеров в localStorage (тоже локально, но «хранится»). Проверить тексты приватности (интро-бейдж, финальный экран) и, где сказано «ничего не сохраняется», поправить на честное «всё остаётся на твоём устройстве». Плюс убедиться, что reset лаборатории чистит сейв (должен — `clearSave()`).

### 5. Дождаться замечаний Codex по диффу
В соседней (моей) сессии крутится Codex-ревью диффа v5 (автосейв: гонки в TELE.push-хуке, восстановление Float32/ImageData из base64, resumeFlow битые состояния). Итог появится в `~/cc/code/itmo/ai-school-workshop/slides-work/`-соседних файлах scratchpad ИЛИ спроси Алексея — он перекинет. Если ревью найдёт баги автосейва — чинить в первом приоритете.

### 6. Опционально (только если всё выше готово и осталось время)
- Кнопка «▶ старт сессии / ⏹ финиш» на дашборде (`/dash`) с маркерами в телеметрию и автозакрытием через 2 часа — просьба Алексея с прогона, отчёты будут резаться по маркерам. Дашборд — боевой, менять АККУРАТНО и только с ревью.
- «Дообучить» (fMore): Алексей на прогоне думал убрать совсем. Сейчас — опциональная кнопка. Решение за Алексеем, не убирать самовольно.

## Definition of Done
1. Все правки — в v5.html; v4.html байт-в-байт нетронут (`git diff v4.html` пуст).
2. E2e зелёные: полный путь (30 PASS) + resume (8 PASS) на v5.
3. Задеплоено `./deploy-ws.sh`, живая проверка `https://ws.meriler.cc/v5.html?ws=1&seat=98` руками: сбор → поломка → разгадка (4712) → починка → финал → опрос (33) → сертификат → лаборатория (клавиши, галерея, reset).
4. F5 посреди урока → «Продолжить с места» работает.
5. После финального смока — СТОП, больше ничего не менять (заморозка до занятия).

## Чего НЕ делать
Не перестраивать state machine и гейты · не менять модель/эмбеддер/пороги KNN · не трогать телеметрию событий (ключи/типы) · не менять коды 4712/33 · не трогать v3/v4 · не деплоить бота (он в порядке) · тесты только на местах 90–99 или ?demo=1.
