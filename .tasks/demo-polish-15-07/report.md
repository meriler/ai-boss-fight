# Отчёт ночной сессии 14.07 — демка к занятию 15.07 готова

**Статус: SHIP (Codex R3). Всё задеплоено на прод. Осталось: утренний смок Алексея → заморозка.**

## Definition of Done спеки — закрыто

1. ✅ Все правки в v5.html; v4.html не тронут (в нём только прогонная правка 13.07 — seat-скоуп unlock, закоммичена как была).
2. ✅ E2e зелёные: полный путь **30 PASS** + resume **8 PASS** (плюс новые: resume2-краевые **9**, home 10, postlesson 37 — итого **94 PASS / 0 FAIL**).
3. ✅ Задеплоено `./deploy-ws.sh`; живой прогон на `https://ws.meriler.cc/v5.html?ws=1&seat=98`: полный путь 29/29 (сбор→поломка→разгадка 4712→починка→финал→опрос 33→сертификат) — playwright по проду.
4. ✅ F5 → «Продолжить с места»: 8/8 на проде (место 97).
5. ⏳ Заморозка — после утреннего смока руками (см. чеклист ниже).

## Задачи спеки

- **1. Экран вариантов** — 5 кнопок по 3–4 слова, равная высота, кегль 18px, ключи `key/cat` не тронуты; правильный не выделяется (проверено скринами + UX-критиком). Разгадка ужата, «Ты всё сделал правильно!» — жирным белым.
- **2. Щёлк на кадр** — мягкая вспышка (10% белого) + 📸 в `addExample` (урок + лаборатория), z-index ПОД карточками/экранами.
- **3. Подсказка починки** — жёлтая строка «📒 Старые примеры остаются в альбоме — ты ДОБАВЛЯЕШЬ новые» в интро t2a/t2b (все варианты текстов).
- **4. Приватность** — аудит: ложных обещаний нет («видео не уходит с устройства» — правда, автосейв только в localStorage), reset чистит сейв. Правок не потребовалось.
- **5. Codex-ревью автосейва** — соседняя сессия ответа не получила (их codex-запуск умер пустым); прогнал сам, **3 круга** (r1 4/10 → r2 6.5/10 → r3 **8.5/10, SHIP**). Полные тексты: `.tasks/demo-polish-15-07/codex/out-r{1,2,3}.md`. Починено 2+2 CRIT и ~10 WARN (детали в git-логе, коммиты 57a3e57…).
- **6. (опция) Пульт занятия на дашборде** — СДЕЛАНО и задеплоено на Aeza (ws-tele перезапущен, active, /dash 200): кнопки «▶ Старт занятия / ⏹ Финиш» под баннером, маркеры в `/var/lib/ws-tele/session-<дата>.json` (JSONL не трогается), автофиниш через 2 ч, атомарная запись. Codex: SHIP. Резка отчётов по маркерам — следующий шаг (rubric.py пока маркеры не читает).
- **«Дообучить» (fMore)** — НЕ трогал, решение за Алексеем (спека).

## Главные фиксы автосейва (почему это было важно)

- Ребёнок без персональной ссылки: все делили один слот `ws_save_x` → теперь ключ считается от выбранного места.
- Старый сейв (<6ч, то же место) + новый прогон = смешанный снапшот → F5 телепортировал на разгадку МИМО замка. Теперь «Поехали» открывает новый цикл (cycleId), вторая вкладка не перезаписывает чужой прогресс.
- F5 во время повторной починки → ложный финал «Почти починил» до второй проверки. Теперь fixdata всегда ведёт на проверку R2.
- Битый сейв ронял boot в «❌ Не загрузилось» с вечным «Повторить». Теперь — тихий чистый старт + очистка слота; восстановление транзакционное (битый хвост не оставляет полу-применённую модель).
- Исход «ИИ не обманулся» после F5 на финале гонял ребёнка по R1 заново → теперь финал сразу.

## Утренний смок (чеклист от Codex R3 — руками, места 90–99 или ?demo=1)

- [ ] две вкладки с одинаковым seat (вторая не должна портить сейв первой);
- [ ] F5 на карточке «ИИ ещё путается» → «Проверим починку ещё раз», лишнего retry нет;
- [ ] fallback-ссылка без `?seat` (если будет раздаваться!) — resume до выбора места не предлагается, это известное ограничение;
- [ ] удержание кнопки класса мышью во время reset / при уходе фокуса — сбор останавливается;
- [ ] все четыре финала (fixed / partial / no_break / inconclusive);
- [ ] слабый ноут: пауза на вехах (сериализация ~300–400КБ) не заметна;
- [ ] пульт занятия на дашборде: ▶ старт в начале урока, ⏹ финиш в конце;
- [ ] полный прогон в РЕАЛЬНОМ Телемосте (микрофон жив при камере тренажёра) → заморозка.

## Не сделано сознательно (после занятия)

- Fallback-resume до выбора места (Codex W7) — завтра все ссылки персональные.
- Резка рубрики/отчётов по маркерам session-файла (маркеры уже пишутся).
- TOCTOU-гонки двух вкладок сузили пере-сверкой, но полностью транзакционным localStorage не бывает — риск признан ничтожным.

## Git

Коммиты (локальные, push не делал — репо ai-boss-fight = GitHub Pages, публикация без явного «да» не нужна):
`57a3e57` круг 1 (задачи 1–4 + e2e на v5) → `5f395d6` круг 2 (ревью Codex r1 + code-reviewer) → `6b0c585` пульт дашборда → `круг 3` (cycleId и транзакционность) → `круг 4` (финал по R3). E2e-раннер теперь отдаёт exit code ≠ 0 при FAIL.
